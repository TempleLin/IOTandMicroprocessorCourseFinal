// Declare and initialize variables
String getTrimmedSubString(String currentLine, String prefixToIgnore, String postfixToIgnore = "HTTP/1.1")
const char *ssid = "MeetingIOTCtrl"
const char *password = "meeting1234"
WiFiServer server = (80)
enum EResponse {  
  SHOW_DOOR_STATUS,
  OPEN_DOOR, 
  CLOSE_DOOR, 
  REGISTER_USER,
  GET_REGISTER_USER,
  LOGIN_USER,
  GET_LOGIN_USER,
  TEST_POST,
  DRINK,
  NONE
}
EResponse eResponse = EResponse::SHOW_DOOR_STATUS
bool doorShouldOpen = false
String extraToPrint = ""
String userIDToRegister = ""
String userIDToLogin = ""
bool userHasDrink = false

// setup function
initialize serial communication
configure access point using ssid and password
start server

// loop function
listen for incoming clients
if client is connected
read incoming data from client
if data is new line 
depending on eResponse variable
- set doorShouldOpen to true or false
- print door status
- register user
- get registered user
- login user
- get login user
- print test post
- print if user has a drink